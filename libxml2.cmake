CMAKE_MINIMUM_REQUIRED( VERSION 3.1 )

include_directories(${CMAKE_CURRENT_SOURCE_DIR}/../3rdLibs/libxml2/libxml2-2.9.2/include)

if(WIN32)
    set(LIBXML2_PATH ${CMAKE_CURRENT_SOURCE_DIR}/../3rdLibs/libxml2/libxml2-2.9.2/bin)
    if(CMAKE_SIZEOF_VOID_P EQUAL 8)
        if(MSVC14)		
		    if(CMAKE_BUILD_TYPE MATCHES Release)			    			
                target_link_libraries(${PROJECT_NAME} ${LIBXML2_PATH}/vc2015_x86_amd64/release/libxml2.lib)				
			else()
			    message("only support release build by now")
		    endif(CMAKE_BUILD_TYPE MATCHES Release)
		else()
		    message("only support msvc14")
	    endif(MSVC14)
	else()
	        message("only support 64bits")
	endif(CMAKE_SIZEOF_VOID_P EQUAL 8)
else()
    message("do not support non windows platform yet")		
endif(WIN32)